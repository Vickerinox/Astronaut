[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
volatile-register = {version = "0.2.2", default-features = false}
bytemuck = {version = "1.25.0", features = ["must_cast"]}